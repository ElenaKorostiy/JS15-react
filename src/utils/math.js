export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
export const mul = (a, b) => a * b;
export const div = (a, b) => a / b;

export const arr = [1, 3, 8, 5, 0, 7, 8]; 

export default { add, sub, mul, div };